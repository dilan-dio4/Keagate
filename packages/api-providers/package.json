{
  "name": "@firagate/api-providers",
  "version": "1.0.0",
  "description": "\"# firagate\"",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build ./tsconfig.package.json",
    "clean": "shx rm -rf build package-lock.json node_modules"
  },
  "dependencies": {
    "@firagate/common": "file:packages/common"
  }
}
